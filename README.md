Домашка по Django-rest
# Домашка первая.
    Заделаны два приложения: materials для курсов и уроков в них (причём уроки положены на с связь с курсами
    через ForeignKey. В курсе много уроков, но каждому уроку свой курс).
    Контроллеры есть пока только для курсов и уроков, для юзеров их нет, пока не
    наделали права доступа.
# Домашка вторая.
    Тут я как то поторопился, видимо, в первой домашке И заделал метод SerielizerMethodFields...
    Вобщем,так получилось, что подсчёт количества курсов уже есть в сериализаторе...
    Но есть нюансы: в контроллере я добавил аннотацию, а в сериализаторе заменил
    соответственно метод и добавлены поля для уроков и подсчёта уроков.
## Модель User
    Пришлось добавить кастомного менеджера для создания супера, без него не хотела никак
    его создавать, писала,типа нет обязательного поля username. А как так его нет, если я его под
    email переопределил....
## Новая модель у юзеров
    Новая модель Payments. Там платежи юзера за урок или курс.
## Кастомные команды
    Пришлось сделать их три штуки: для заполнения юзерами, материалами и платежами.
# Домашка третья
    Вот мы и добрались до прав...
    Заменены...всё! Модели, сериализаторы, контроллеры, пути...В обоих приложениях.
# Домашка четвёртая
    Ну ладно, пагинация и валидация не так уж сложно.
    Но, мать их, грёбаные тесты!!!!!!!!!
    Да видал я их....
    Они бесили меня на функционалке и я уж думал, прошли этот ужас и хрен с ним.
    Ага..Щаззз!!!
# Домашка пятая
    Блин, реально сложная. Я баран, мне надо на пальцах обьяснять как работает API,
    в документации к Stripe сам чёрт ногу сломит.
## Изменения в модели Course
    Для Stripe добавлены поля цены, идентификатора продукта и идентификатора цены.
## Новый файл docs.py
    Для генерации документирования. А вот нахрена оно надо, я не понял совсем.
## Пагинация
    Тут в моделях просто добавлено поле  ordering в мету, Сделан новый файл с пагинаторами
    а в контроллеры добавлены поля соответствующих классов пагинаторов.
## Грёбаное API
    Вот тут самое корявое наделано. Для этого наделаны сериализаторы модели платежей.
    Сама модель в приложении users забабахана ещё во второй домашке.
    Вот по контроллерам прошлись знатно. Ну новый контроллер для страйпа дело ясное,
    Но вот ещё потребовалось сделать декоратор контроллера курсов на создание
    платёжной сессии. И, ессесно, урлы для этих контроллеров
### Вебхук (Webhook)
    Хрен его знает, что это и зачем, но вот оно нужно. Код для этой штуки целиком спионерил
    у страйпа на странице верстака. И, понятное дело, ключи спрятаны в файл .env
# Домашка шестая
    Хоть ветка и названа feature_homework5, но это уже шестая. Чуствую усталость...
    Вот тут мне было прям очень интересно, как отправлять всякие там майлики и рекламки, как
    в ютубе.
## Config
    Ну тут всё понятно. Всё содрано с уроков и частично с доков для celery.
    Особенно в settings.py
## Materials
    Тут, собсннаа, в модели лёгкие переменки (поле updated_at добавилось). В контроллерах даписаны вызовы задач.
    Сами задачи нашлёпаны в модуле tasks.py
## test_data
    Это я модуль залепил, чтобы затестить, как работает и работает ли вообще выполнение задач.
## Signals.py и utils.py
    В этих модулях лежат проверка наличия обновления и необходимости отправки об нём уведомления
    Вот не уверен,что они  должны быть отдельно в корне. По моему, их надо было пихать
    в materials.
## Docker
    Запуск контейнера осуществляется командой "docker-compose up --build".
    Остановка контейнера командой "docker-compose down".
### Проверка работоспособности контейнера
Проверьте веб-приложение: Откройте http://localhost:8000/

Проверьте админку: http://localhost:8000/admin/

Проверьте API: http://localhost:8000/api/courses/
               http://localhost:8000/api/lessons/

Проверить Celery:
 Откройте http://localhost:8000/admin/

Перейдите в раздел "Django Celery Beat"

Проверьте:

Periodic tasks - настроенные периодические задачи

Crontabs - расписания

Intervals - интервалы

#### Если не подгружается статика
1. Набрать в контейнере команду
docker-compose exec web python manage.py collectstatic --noinput
2. Перезагрузить страницу в браузере
## Nginx и deploy
   Для сборки статики сделан новый файл nginx.conf
   Слегка изменены Dockerfile и docker-compose с учётом этого
   Для выгрузки приложения сделан файл deploy.yml. В нём спецом указаны ветки
   и main и develop и feature для того, чтобы можно было проверить workflow
   на гите. Иначе проверка будет возможна только после слияния веток в develop.
## Инструкция по настройке сервера и деплоя
    Будет много букавок...
### Настройка сервера
1. Подготовка сервера
Создайте VM в Yandex Cloud
Настройте SSH доступ
Обновите систему: sudo apt update && sudo apt upgrade -y
2. Установка Docker
Чтобы установить Docker, воспользуйтесь инструкцией по установке с официального
сайта: https://docs.docker.com/engine/install/ubuntu/#install-using-the-repository.
3. Настройка проекта
Клонируйте репозиторий. Наберите в командной строке поочерёдно следующие команды:
    cd /var/www
    sudo git clone https://github.com/your-username/your-repo.git
    sudo chown -R $USER:$USER your-repo/
    cd your-repo
your-username замените на имя вашего пользователя в акке гита
your-repo замените на название вашего репозитория в гите
4. Создание .env файла
#### Создайте .env файл с настройками
    Опять же, в командной строке:
cat > .env << EOF
DEBUG=False
SECRET_KEY=your-secret-key-here
DATABASE_NAME=your_db_name
DATABASE_USER=your_db_user
DATABASE_PASSWORD=your_secure_password
ALLOWED_HOSTS=your-server-ip,localhost,127.0.0.1
REDIS_URL=redis://redis:6379/0
EOF

#### Настройте права
    CMD наше всё...:
chmod 600 .env
### GitHub Actions
1. Добавление Secrets в репозиторий
В GitHub: Settings → Secrets and variables → Actions → New repository secret
Необходимые secrets:
SERVER_HOST - IP адрес сервера
SERVER_USER - имя пользователя на сервере (логин, а не название ВМ)
SSH_KEY - приватный SSH ключ
2. Создание SSH ключа
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/deploy_key
3. Добавление публичного ключа на сервер
#### Скопируйте публичный ключ
cat ~/.ssh/deploy_key.pub
#### На сервере добавьте в authorized_keys
echo "ssh-ed25519 AAA... github-actions" >> ~/.ssh/authorized_keys
### Запуск приложения
    Ну, я думаю, понятно, где это набирать:
cd /var/www/your-repo
#### Сборка и запуск
docker compose up --build -d
#### Остановка
docker compose down
### Проблемы с правами Docker
#### Добавить пользователя в группу docker
    Наша любимая CMD:
sudo usermod -aG docker $USER
newgrp docker
### Если кэш контейнера переполнится:
    И как бы я жил без CMD:
#### Удалить неиспользуемые образы
docker system prune -f
#### Полная очистка
docker system prune -a -f
